pipeline {
    agent { label 'docker-agent' }

    stages {
        stage('Clone Info') {
            steps {
                sh 'echo "Code pulled successfully!"'
                sh 'ls -la'
            }
        }
    }
}
